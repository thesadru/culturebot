"""Config but knda like dataclasses.

It's like the japanese 設定 [settei] (eng. config) but I spell it with a single t cause settei is already taken on pypi.
"""
from .config import *
from .fields import *
